function setNavMenuHidden(hidden) {
    // Get element with the specified ID name
    var navmenu = document.getElementById("navmenu");
    navmenu.hidden = hidden;
}
function alert(text) {
    window.alert(text)
}